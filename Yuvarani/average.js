// const arr = [1, 2, 3, 4, 5];

// var sum = 0;

// for (var number of arr) {
//     sum += number;
// }

// average = sum / arr.length;

// console.log("The average of given numbers is",average);


const arr = [1, 2, 12, 4, 10];

var sum = 0;

arr.forEach(function(num) { sum += num });

average = sum / arr.length;

console.log("The average of given numbers is",average);