function calculateArea(length, width) {

    const result = (length* width); 
    return result;
}

const length = 2;
const width = 6;

console.log("Area of rectangle",calculateArea(length, width)); 
