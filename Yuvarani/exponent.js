function calculatePower(base, exponent) {

    const result = Math.pow(base, exponent); //base**exponent
    return result;
}

const base = 2;
const exponent = 3;
console.log(calculatePower(base, exponent)); 
